export default interface IConfig {
    header: {
        height: number;
        minHeight: number;
        maxHeight: number;
        fontSize: number;
        minFontSize: number;
        maxFontSize: number;
    }
}